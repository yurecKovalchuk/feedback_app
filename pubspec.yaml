name: feedback_app
description: Contact form for feedback

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  go_router: ^12.1.1
  logging: ^1.2.0
  get_it: ^7.6.4
  injectable: ^2.3.0
  cupertino_icons: ^1.0.2
  freezed: ^2.4.5
  freezed_annotation: ^2.4.1
  json_serializable: ^6.7.1
  http: ^1.1.0

dev_dependencies:
  flutter_lints: ^3.0.0
  injectable_generator: ^2.4.1
  build_runner: ^2.4.6
  flutter_gen_runner: ^5.3.2

flutter:
  uses-material-design: true
